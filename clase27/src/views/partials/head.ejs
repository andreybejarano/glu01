<!DOCTYPE html>
<html>
  <head>
    <title>Pimienta & sal</title>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <link rel="stylesheet" href="/css/style.css" />
    <% if (locals.styles && Array.isArray(locals.styles)) {
        styles.forEach(style => { %>
            <link rel="stylesheet" href="/css/<%= style %>.css">
        <% })
    } %>
  </head>